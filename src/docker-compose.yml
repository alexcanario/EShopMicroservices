services:
  catalogdb:
    image: postgres

  basketdb:
    image: postgres

  distributedcache:
    image: redis

  orderdb:
    image: mcr.microsoft.com/mssql/server

  messagebroker:
    image: rabbitmq:management

  catalog.api:
    image: ${DOCKER_REGISTRY-}eshopcatalogapi
    build:
      context: .
      dockerfile: Services/Catalog/EShop.Catalog.Api/Dockerfile

  basket.api:
    image: ${DOCKER_REGISTRY-}eshopbasketapi
    build:
      context: .
      dockerfile: Services/Basket/EShop.Basket.Api/Dockerfile

  discount.grpc:
    image: ${DOCKER_REGISTRY-}eshopdiscountgrpc
    build:
      context: .
      dockerfile: Services/Discount/EShop.Discount.Grpc/Dockerfile

  ordering.api:
    image: ${DOCKER_REGISTRY-}eshoporderingapi
    build:
      context: .
      dockerfile: Services/Ordering/EShop.Ordering.API/Dockerfile

volumes:
  postgres_catalog:
  postgres_basket:
  sqlserver_order:  
  rabbitmq_data:
